import { computeValue } from "../../../core/_internal";
import { truthy } from "../../../util/_internal";
function $expr(_, expr, options) {
  return (obj) => truthy(computeValue(obj, expr, null, options), options.useStrictMode);
}
export {
  $expr
};
