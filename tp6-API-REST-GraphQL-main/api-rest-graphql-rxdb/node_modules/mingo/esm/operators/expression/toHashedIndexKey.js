import { computeValue } from "../../core/_internal";
import { hashCode } from "../../util";
const $toHashedIndexKey = (obj, expr, options) => hashCode(computeValue(obj, expr, null, options));
export {
  $toHashedIndexKey
};
