import { WindowOperator } from "../../types";
/**
 * Fills null and missing fields in a window using linear interpolation based on surrounding field values.
 */
export declare const $linearFill: WindowOperator;
