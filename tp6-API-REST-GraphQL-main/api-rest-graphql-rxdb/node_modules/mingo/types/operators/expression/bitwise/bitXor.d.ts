import { ExpressionOperator } from "../../../types";
/**
 * Returns the result of a bitwise xor (exclusive or) operation on an array of int and long values.
 */
export declare const $bitXor: ExpressionOperator;
