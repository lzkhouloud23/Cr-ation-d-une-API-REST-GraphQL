import { Any, AnyObject, Options } from "../../../types";
export declare function processBitwise(obj: AnyObject, expr: Any, options: Options, operator: string, compute: (n: number[]) => number): number;
