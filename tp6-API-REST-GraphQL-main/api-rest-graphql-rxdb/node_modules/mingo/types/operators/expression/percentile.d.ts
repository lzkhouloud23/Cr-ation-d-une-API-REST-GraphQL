import { ExpressionOperator } from "../../types";
/**
 * Returns an array of scalar values that correspond to specified percentile values.
 */
export declare const $percentile: ExpressionOperator<number[]>;
