import { ExpressionOperator } from "../../../types";
/**
 * Removes whitespace characters, including null, or the specified characters from the beginning of a string.
 */
export declare const $ltrim: ExpressionOperator;
