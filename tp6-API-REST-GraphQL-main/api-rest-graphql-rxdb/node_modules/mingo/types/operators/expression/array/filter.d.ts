import { ExpressionOperator } from "../../../types";
/**
 * Selects a subset of the array to return an array with only the elements that match the filter condition.
 */
export declare const $filter: ExpressionOperator;
