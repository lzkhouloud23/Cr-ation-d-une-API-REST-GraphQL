import { ExpressionOperator } from "../../../types";
/**
 * Applies an expression to each element in an array and combines them into a single value.
 */
export declare const $reduce: ExpressionOperator;
