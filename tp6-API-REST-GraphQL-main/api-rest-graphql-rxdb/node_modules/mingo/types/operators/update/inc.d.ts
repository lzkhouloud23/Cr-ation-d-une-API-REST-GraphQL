import { AnyObject, Options } from "../../types";
/** Increments a field by a specified value. */
export declare const $inc: (expr: Record<string, number>, arrayFilters?: AnyObject[], options?: Options) => (obj: AnyObject) => string[];
