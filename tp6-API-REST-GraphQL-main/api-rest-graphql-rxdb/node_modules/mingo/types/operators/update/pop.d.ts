import { AnyObject, Options } from "../../types";
/** Removes the first or last element of an array. */
export declare const $pop: (expr: Record<string, 1 | -1>, arrayFilters?: AnyObject[], options?: Options) => (obj: AnyObject) => string[];
