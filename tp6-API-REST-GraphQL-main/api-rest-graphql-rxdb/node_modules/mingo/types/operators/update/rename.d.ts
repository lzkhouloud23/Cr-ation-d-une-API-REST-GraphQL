import { AnyObject, Options } from "../../types";
/** Replaces the value of a field with the specified value. */
export declare const $rename: (expr: Record<string, string>, arrayFilters?: AnyObject[], options?: Options) => (obj: AnyObject) => string[];
