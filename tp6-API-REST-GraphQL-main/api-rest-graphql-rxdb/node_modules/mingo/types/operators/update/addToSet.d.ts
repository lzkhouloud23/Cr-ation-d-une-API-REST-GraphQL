import { AnyObject, Options } from "../../types";
/** Adds a value to an array unless the value is already present. */
export declare const $addToSet: (expr: AnyObject, arrayFilters?: AnyObject[], options?: Options) => (obj: AnyObject) => string[];
