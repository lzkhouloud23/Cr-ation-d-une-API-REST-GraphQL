import { AnyObject, Options } from "../../types";
/** Appends a specified value to an array. */
export declare const $push: (expr: AnyObject, arrayFilters?: AnyObject[], options?: Options) => (obj: AnyObject) => string[];
