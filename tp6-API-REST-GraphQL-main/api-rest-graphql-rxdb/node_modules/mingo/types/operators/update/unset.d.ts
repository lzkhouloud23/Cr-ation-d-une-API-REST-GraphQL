import { AnyObject, Options } from "../../types";
/** Deletes a particular field */
export declare const $unset: (expr: Record<string, "">, arrayFilters?: AnyObject[], options?: Options) => (obj: AnyObject) => string[];
