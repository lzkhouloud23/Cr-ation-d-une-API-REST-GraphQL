import { AnyObject, Options } from "../../types";
/** Multiply the value of a field by a number. */
export declare const $mul: (expr: Record<string, number>, arrayFilters?: AnyObject[], options?: Options) => (obj: AnyObject) => string[];
