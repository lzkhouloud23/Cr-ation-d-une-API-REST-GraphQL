import { AnyObject, Options } from "../../types";
/** Replaces the value of a field with the specified value. */
export declare const $set: (expr: AnyObject, arrayFilters?: AnyObject[], options?: Options) => (obj: AnyObject) => string[];
